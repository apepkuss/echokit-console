# 服务器配置
SERVER_ADDR=0.0.0.0
SERVER_PORT=3000

# Docker 配置
DOCKER_IMAGE=secondstate/echokit:latest-server-vad
CONFIG_DIR=./data/configs
RECORD_DIR=./data/records
HELLO_WAV_PATH=./data/hello.wav
PORT_RANGE_START=8080
PORT_RANGE_END=8180

# 数据库配置
DATABASE_URL=postgres://echokit:echokit@localhost:5432/echokit

# Redis 配置（用于设备激活码缓存）
REDIS_URL=redis://localhost:6379
ACTIVATION_TTL=300

# Proxy WebSocket URL（设备激活成功后返回给设备的 Proxy 地址）
# 注意：设备端目前会忽略此字段，使用用户配网时指定的地址
# 如果需要服务器端指定，请设置为实际的 Proxy 地址
PROXY_WS_URL=ws://192.168.0.103:10086/ws

# 外部访问地址（可选）
# 用于替换容器 WebSocket URL 中的 localhost
# 设置为服务器的实际 IP 地址或域名，以便外部设备访问
EXTERNAL_HOST=192.168.0.103
